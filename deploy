delete files under src/egg-info
delete files under dist/*

rm dist/*

py -m build

del dist/*
py -m twine upload --repository testpypi --skip-existing  dist/*
py -m twine upload --repository pypi --skip-existing  dist/*
